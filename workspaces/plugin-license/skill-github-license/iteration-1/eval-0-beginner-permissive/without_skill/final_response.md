จัดให้แล้วครับ เลือก **MIT License** ให้ — ตรงกับที่บอกว่า "ใครจะเอาไปทำอะไรก็ได้ เอาไปขายก็ไม่ว่า" ที่สุด

## MIT คืออะไร (สรุปสั้นๆ พอ)

อ่านจบใน 30 วินาที เนื้อหาจริงๆ มีแค่ 2 ข้อ

1. **ใครก็เอาไปทำอะไรก็ได้** — ใช้ แก้ ขาย เอาไปรวมกับโปรแกรมปิด source ได้หมด ไม่ต้องขออนุญาต ไม่ต้องจ่ายเงิน
2. **แลกกับ 2 อย่าง** — เขาต้องคงชื่อคุณกับข้อความ license ไว้ในโค้ดที่เอาไป และ **ถ้าโปรแกรมพัง เขาฟ้องคุณไม่ได้**

ข้อ 2 ท่อนหลังสำคัญกว่าที่คิดครับ ถ้าไม่ใส่ license อะไรเลย คุณไม่ได้ "ใจดี" แต่กลายเป็น **"สงวนลิขสิทธิ์ทั้งหมด"** โดยอัตโนมัติตามกฎหมาย แปลว่าตามหลักแล้วไม่มีใครมีสิทธิ์เอาไปใช้เลย — บริษัทที่มีฝ่ายกฎหมายจะเลี่ยง repo ที่ไม่มี license ทันที ฉะนั้นการใส่ MIT คือสิ่งที่ทำให้ "มีคนใช้เยอะ" อย่างที่คุณอยากได้จริงๆ

## แก้อะไรไปบ้าง (4 จุด)

| ไฟล์ | ทำอะไร |
|---|---|
| `LICENSE` | **สร้างใหม่** — ข้อความ MIT ฉบับเต็ม ระบุ `Copyright (c) 2026 Natthasath Saksri` |
| `pyproject.toml` | เพิ่ม `license = "MIT"`, `license-files`, และ `authors` เพื่อให้ข้อมูลติดไปกับ package ตอนขึ้น PyPI |
| `README.md` | เพิ่ม section `📄 License` ท้ายไฟล์ |
| `pyproject.toml` (build) | ปรับ `hatchling` → `hatchling>=1.27` ระบุเวอร์ชันขั้นต่ำไว้ให้ชัด เพื่อให้ฟิลด์ `license` แบบใหม่ (PEP 639) ทำงานได้แน่นอนทุกเครื่อง (ของเดิมไม่ได้พัง แค่กันเหนียวไว้) |

ที่ต้องใส่ทั้ง `LICENSE` และ `pyproject.toml` เพราะมันคนละที่กัน — ไฟล์ `LICENSE` คือสิ่งที่ GitHub อ่านแล้วขึ้นป้าย "MIT" ข้างๆ repo ส่วน `pyproject.toml` คือสิ่งที่ติดไปกับ `pip install qrsnap` ให้คนเห็นบนหน้า PyPI

## เช็คให้แล้วอีกอย่าง

library ที่คุณใช้ — `requests` (Apache-2.0), `click` (BSD), `qrcode` (BSD) — เป็นแบบ permissive หมด ไม่มีตัวไหนเป็น GPL ที่จะบังคับให้โปรเจกต์คุณต้องเปลี่ยน license ตาม ฉะนั้นปล่อย MIT ได้สบายใจครับ

## ก่อนกด push

ปีใน `LICENSE` ใส่ 2026 ไว้ — ถ้า qrsnap เคยปล่อยออกไปตั้งแต่ปีก่อนแล้ว (เห็นเลขเวอร์ชันเป็น 0.3.1) แก้เป็น `2025-2026` ได้เลย แก้บรรทัดเดียวพอ

> เผื่ออนาคต: ถ้าวันหนึ่งโปรเจกต์โตจนมีบริษัทใหญ่เอาไปใช้ แล้วคุณอยากได้การคุ้มครองเรื่องสิทธิบัตรเพิ่ม ค่อยขยับไป **Apache-2.0** ได้ — เสรีเท่า MIT ทุกอย่าง แค่ยาวกว่าและมีข้อสัญญาเรื่อง patent เพิ่มมา แต่ตอนนี้ MIT เหมาะกับ CLI tool ขนาดนี้ที่สุดแล้วครับ
